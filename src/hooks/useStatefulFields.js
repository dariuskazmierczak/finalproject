//import React, { useState } from 'react';

/*export function useStatefulFields() {
    const [values, setValues] = useState({}); //similar logic to handleChange

    const handleChange = e => {
        setValues({
            [e.target.name]: e.target.value
        })
    }

    return [values, handleChange]; //we can put also in object

}*/